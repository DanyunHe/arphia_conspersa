# Core scientific computing
numpy>=1.24.0,<2.0
pandas>=2.0.0
numba>=0.57.0
scipy>=1.10.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0
scikit-image>=0.21.0
rawpy>=0.19.0
tifffile>=2023.1.0
imageio>=2.28.0

# File I/O
openpyxl>=3.1.0

# Visualization
matplotlib>=3.7.0

# PyTorch (required for DeepLabCut 3.x, SAM, and Cellpose)
# Install the appropriate version for your system from https://pytorch.org
#   Linux/Windows CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CPU-only:           pip install torch torchvision
#   macOS (Apple Silicon or Intel): pip install torch torchvision
torch>=2.0.0
torchvision>=0.15.0

# DeepLabCut for pose estimation (Step 1)
# Version 3.x uses PyTorch backend — no TensorFlow needed
deeplabcut>=3.0.0rc5

# Segment Anything Model (SAM) for wing extraction (Step 2)
# Install from GitHub: pip install git+https://github.com/facebookresearch/segment-anything.git

# Cellpose for domain segmentation (Step 4)
# Note: cellpose 4.x also pulls in segment-anything as a dependency
cellpose>=4.0.0

# Geometry and graph processing (Step 5)
shapely>=2.0.0
networkx>=3.0
